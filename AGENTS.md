# Agents

## 环境

- uv: 0.10.6
- Python: 3.14.3

## 规范

### 关于 `core_*` 文件与 `util_*` 文件

- 在模块级 `__init__.py` 中只能导出 `core_*` 开头的文件。
- 以 `core_*` 代码为主, 如果 `util_*` 代码与前者冲突, 优先修改 `util_*` 代码。