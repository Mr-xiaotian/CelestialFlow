# UtilsCollections

`utils/util_collections.py` provides collection/dictionary clustering tools.

## Main Functions

- `cluster_by_value_sorted(input_dict)`: Clusters by value and returns results sorted in ascending order by value.
