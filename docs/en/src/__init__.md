# CelestialFlow Package Entry

> 📅 Last Updated: 2026/06/05

## Purpose

Documents the package-level public API exported from `celestialflow.__init__`, so users can understand which graph, stage, web, persistence, and utility symbols are intended for direct import.

## Key Points

- Summarizes the main export groups: graph, stage, observability, utils, web, persistence, and runtime.
- Explains that `from celestialflow import ...` is the main high-level import path.
- Serves as the index page for package-level public symbols.
