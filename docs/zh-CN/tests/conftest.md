# 全局测试配置 (conftest.py)

> 最后更新日期: 2026/05/23

## 作用
作为整个 `tests/` 目录的根级配置文件，负责初始化测试环境所需的全局变量和环境变量。

## 核心功能
- **环境变量加载**: 自动调用 `dotenv.load_dotenv()`，确保项目根目录下的 `.env` 文件中的配置（如 API 密钥、数据库连接串等）在测试启动时可用。

## 注意事项
- 该文件会自动被 Pytest 识别。
- 如果需要添加全局级别的 Fixture，应在此文件中定义。
