# UtilsDebug

> 📅 最終更新日: 2026/05/09

`utils/util_debug.py` はデバッグ補助関数を提供します。

## 主要関数

- `find_unpickleable(obj)`：オブジェクトが pickle 可能かどうかを検査し、失敗時に診断情報を出力します。

オブジェクトのシリアライゼーション問題のトラブルシューティングによく使用されます（例：データをプロセス間で渡す必要がある場合や永続化が必要な場合）。
