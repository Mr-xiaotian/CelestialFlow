# util_cal

> 📅 最終更新日: 2026/04/22

Web モジュールの計算ユーティリティ関数です。

## cal_interval

```python
def cal_interval(refresh_interval: int) -> float:
    """ミリ秒のリフレッシュ間隔を秒に変換し、[1, 60] の範囲に制限します。"""
```

フロントエンドから渡されたミリ秒単位のリフレッシュ間隔を秒に変換し、適切な範囲に制限します。過度に頻繁な、または過度にまばらなポーリングを防止します。
