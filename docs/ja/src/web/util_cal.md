# util_cal

> 📅 最終更新日: 2026/04/22

Web モジュールの計算ユーティリティ関数。

## cal_interval

```python
def cal_interval(refresh_interval: int) -> float:
    """ミリ秒単位のリフレッシュ間隔を秒に変換し、[1, 60] の範囲に制限する。"""
```

フロントエンドから渡されるミリ秒単位のリフレッシュ間隔を秒に変換し、適切な範囲に制限することで、過度に頻繁または過度に疎なポーリングを防止します。
