# globals.d.ts

> 📅 最終更新日: 2026/06/05

## 目的

CDN で読み込むライブラリやモジュール横断ヘルパーのための TypeScript グローバル宣言を説明します。

## 主要ポイント

- `Chart`、`Sortable`、`window.mermaid`、`Lang`、`currentLang`、`setLang`、`t`、`applyI18nDOM` を対象にします。
- 非モジュール型グローバルを TypeScript コンパイラから見えるようにします。
